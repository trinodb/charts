{{- if .Values.routingRules.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: routing-rules
  labels:
    {{- include "trino-gateway.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.routingRules.config }}
  {{$key }}: |-
    {{- $value | nindent 4 }}
{{- end }}
{{- end }}
